format_version: 4
pipelines:
  "dashboard-pipeline-gocd-trigger":
    group: gp2gp-data-pipeline-infra
    label_template: "${metrics_calculator_task[:8]}"
    materials:
      metrics_calculator_task:
        plugin_configuration:
          id: git-path
        options:
          url: https://github.com/nhsconnect/prm-gp2gp-data-pipeline-infra.git
          path: stacks/dashboard-pipeline-gocd-trigger
    stages:
      - validate:
          clean_workspace: true
          jobs:
            test:
              resources:
                - docker
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-validate dashboard-pipeline-gocd-trigger dev
      - plan_dev:
          clean_workspace: true
          jobs:
            plan:
              resources:
                - docker
              artifacts:
                - build:
                    source: dev.tfplan
                - build:
                    source: lambda/build/dashboard-pipeline-gocd-trigger.zip
                    destination: lambda/build/
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-plan dashboard-pipeline-gocd-trigger dev
      - apply_dev:
          clean_workspace: true
          jobs:
            apply:
              resources:
                - docker
              tasks:
                - fetch:
                    stage: plan_dev
                    job: plan
                    source: dev.tfplan
                    is_file: yes
                - fetch:
                    stage: plan_dev
                    job: plan
                    source: lambda/build/dashboard-pipeline-gocd-trigger.zip
                    destination: lambda/build/
                    is_file: yes
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-apply dashboard-pipeline-gocd-trigger dev
      - validate_preprod:
          clean_workspace: true
          approval:
            type: manual
            allow_only_on_success: true
          jobs:
            plan:
              resources:
                - docker
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-validate dashboard-pipeline-gocd-trigger preprod
      - plan_preprod:
          clean_workspace: true
          jobs:
            plan:
              resources:
                - docker
              artifacts:
                - build:
                    source: preprod.tfplan
                - build:
                    source: lambda/build/dashboard-pipeline-gocd-trigger.zip
                    destination: lambda/build/
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-plan dashboard-pipeline-gocd-trigger preprod
      - apply_preprod:
          clean_workspace: true
          approval:
            type: manual
            allow_only_on_success: true
          jobs:
            apply:
              resources:
                - docker
              tasks:
                - fetch:
                    stage: plan_preprod
                    job: plan
                    source: preprod.tfplan
                    is_file: yes
                - fetch:
                    stage: plan_prod
                    job: plan
                    source: lambda/build/dashboard-pipeline-gocd-trigger.zip
                    destination: lambda/build/
                    is_file: yes
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - ./tasks dojo-apply dashboard-pipeline-gocd-trigger preprod
